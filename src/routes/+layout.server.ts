import type { LayoutServerLoad } from "./$types"

export const load: LayoutServerLoad = async (event) => {

  // let token = event.locals.jwt;

  // console.log("tok", await event.locals.getSession());
  
  // return {
  //   session: token,
  // }
}